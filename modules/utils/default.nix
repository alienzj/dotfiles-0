{
  programs.aria2 = {
    enable = true;
  };

  programs.bat = {
    enable = true;
  };

  programs.less = {
    enable = true; 
  };

  programs.lesspipe = {
    enable = true; 
  };

  programs.exa = {
    enable = true;
  };

  programs.lsd = {
    enable = true;
  };

  programs.htop = {
    enable = true;
  };

  programs.jq = {
    enable = true;
  };

  programs.nnn = {
    enable = true;
  };

  programs.pandoc = {
    enable = true;
  };

  programs.tmux = {
    enable = true;
  };
 
}
